{% extends 'base.html.twig' %}
{% block body %}
    <div>
        <h2>{{ user.username }}</h2>
        <h2>投稿</h2>
        {% for post in user.userPosts %}
            <h3>{{ post.title }}</h3>
            <p>{{ post.description }}</p>
        {% endfor %}
    </div>
{% endblock %}